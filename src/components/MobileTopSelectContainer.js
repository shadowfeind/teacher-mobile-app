import React from "react";

const MobileTopSelectContainer = ({ children }) => {
  return (
    <div style={{ padding: "16px", borderBottom: "1px solid #d3d3d3" }}>
      {children}
    </div>
  );
};

export default MobileTopSelectContainer;
